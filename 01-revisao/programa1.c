#include <stdio.h>
#include <stdlib.h>
#include <time.h>

int main() {
    //system("clean"); //limpar a tela no linux - clear
    system("cls"); //limpar a tela no windows - cls
    printf("Boa noite turma\n");
    return 1;
}


//compilando um arquivo .C -> gcc nomeArquivo.c -o programa
//Executando esse arquivo -> ./programa